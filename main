import tweepy
import time

consumer_key = None 
consumer_secret = None 
access_token = None 
access_token_secret = None 

# everything above is pending to be approved >:(

auth = tweepy.OAuthHandler(consumer_key, consumer_secret)
auth.set_access_token(access_token, access_token_secret)
api = tweepy.API(auth) # insert authorization
public_tweets = api.home_timeline() # a copy of the timeline in my twitter account

hydr_message = "Don't forget to drink water and stay hydrated!"
hydr_img = 'turtles.jpg'

def publish_test(text):
	api.update_status(text)
publish_test("hello world")

'''method = True 
while method == True:
	API.update_with_media(hydr_img[hydr_message])
	time.sleep(3600) # an hour has 3600 seconds '''
